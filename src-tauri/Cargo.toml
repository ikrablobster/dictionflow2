[package]
name = "dictaflow"
version = "0.1.0"
description = "Real-time offline-first voice dictation with smart punctuation for Windows"
authors = ["DictaFlow"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-store = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-clipboard-manager = "2"
tauri-plugin-autostart = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
thiserror = "1"

# Аудио захват с микрофона (кроссплатформенно)
cpal = "0.15"

# Определение речевой активности (энергетический VAD как fallback,
# при наличии сети в CI подключается модель Silero через onnxruntime)
webrtc-vad = "0.4"

# Симуляция ввода с клавиатуры / буфер обмена — вставка текста в активное окно
enigo = "0.2"

# Глобальные хоткеи на низком уровне (доп. к плагину tauri, нужно для press&hold)
rdev = "0.5"

# Локальная база истории диктовок
rusqlite = { version = "0.32", features = ["bundled"] }

# Для облачного режима (опционально): HTTP-клиент
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }

chrono = { version = "0.4", features = ["serde"] }
regex = "1"
dirs-next = "2"

# ---------------------------------------------------------------------------
# whisper-rs — офлайн-распознавание речи (whisper.cpp).
# ВАЖНО: фича "metal" — это GPU-ускорение через Apple Metal framework,
# доступна ТОЛЬКО на macOS (требует Foundation.framework). Если включить её
# безусловно для всех платформ, CMake падает на Windows/Linux с ошибкой
# "Could not find FOUNDATION_LIBRARY" ещё на этапе конфигурации сборки.
# Поэтому объявляем зависимость раздельно по таргету.
# ---------------------------------------------------------------------------
[target.'cfg(target_os = "macos")'.dependencies]
whisper-rs = { version = "0.13", features = ["metal"], optional = true }

[target.'cfg(not(target_os = "macos"))'.dependencies]
whisper-rs = { version = "0.13", optional = true }

[features]
default = ["local-whisper"]
local-whisper = ["dep:whisper-rs"]

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_UI_WindowsAndMessaging",
    "Win32_Foundation",
    "Win32_System_Threading",
    "Win32_UI_Input_KeyboardAndMouse",
] }
clipboard-win = "5"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = 3
strip = true
